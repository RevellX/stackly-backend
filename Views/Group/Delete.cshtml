@model StacklyBackend.Models.Group;

@{
    ViewData["Title"] = "Delete";
}

<h2>Group - @ViewData["Title"]</h2>
<a href="/group"><button type="button" aria-label="Go back">Go to list</button></a>
<p>Item delete confirmation form: </p>
<form action="/group/delete/@Model.Id" method="post">
    @Html.AntiForgeryToken()
    <div>
        <p>Are you sure you wan to delete @Model.Name?</p>
    </div>
    <a href="/group"><button type="button" aria-label="Deny">No</button></a>
    <button type="submit">Yes</button>
</form>